{
	"queries": [
		{
			"name": "sample.list.methods",
			"description": "List all methods",
			"query": "MATCH (m:METHOD_DEF) RETURN m.name as msg"
		},
		{
			"name": "sample.list.classes",
			"description": "List all classes",
			"query": "MATCH (c:CLASS_DEF) RETURN c.simpleName as msg"
		}
	]
}
